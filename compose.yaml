services:
  api:
    build: .
    container_name: sherlocklabs-api
    expose:
      - "5000"

  nginx:
    image: nginx:alpine
    container_name: sherlocklabs-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
